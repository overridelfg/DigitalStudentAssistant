<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.digitalstudentassistant">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DigitalStudentAssistant">
        <activity android:name=".ui.cv.CVActivity"></activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".ui.projectdetails.ProjectDetailsActivity" />
        <activity
            android:name=".ui.ProjectsActivity"
            android:label="@string/title_activity_projects"
            android:theme="@style/Theme.DigitalStudentAssistant.NoActionBar" />
        <activity
            android:name=".ui.SplashActivity"
            android:configChanges="orientation"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>